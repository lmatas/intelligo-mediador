export class IntelligoNode {
    label: string;
    s: number;
    cid: number;
    id: number;

    constructor(label: string, s: number, cid: number, id: number) {
        this.label = label;
        this.s = s;
        this.cid = cid;
        this.id = id;
    }
}